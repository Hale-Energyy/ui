import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.hale',
  appName: 'android-ui',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
